# Datenbank-Verbindung
DATABASE_URL="mysql://root:password@localhost:3306/nutzerkosten_db"

# Beispiel für verschiedene Umgebungen:
# DATABASE_URL="mysql://username:password@localhost:3306/database_name"
# DATABASE_URL="mysql://user:pass@host:port/db"

# Hinweise:
# - Ersetze 'password' mit deinem MySQL-Passwort
# - Ersetze 'database_name' mit deinem Datenbank-Namen
# - Für Produktion: Verwende sichere Passwörter und separate Datenbank

# App-URL (für Magic-Link-Generierung)
APP_URL="http://localhost:4321"  # Development
# APP_URL="https://nutzerkosten.wuestenstein.de"  # Production

# Node Environment (beeinflusst Email-Versand)
NODE_ENV="development"  # "development" = Console-Log, "production" = SMTP

# SMTP-Konfiguration (nur für Production erforderlich)
SMTP_HOST="smtp.all-inkl.com"
SMTP_PORT="587"
SMTP_SECURE="false"  # "true" für Port 465, "false" für Port 587
SMTP_USER="deine@email.de"
SMTP_PASS="dein-smtp-passwort"
SMTP_FROM="noreply@wuestenstein.de"

# Weather Cache Service (Wetter + Indoor-Temperaturen)
WEATHER_CACHE_URL="http://100.102.83.46:3003/api"

# Push-Benachrichtigungen (VAPID Keys)
# Generiere neue Keys mit: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY="BPNNrS_We0xC472MxEYFKhQeaOsvwOkD41X5KPe0sbw43MH2NyXYSYORxJ0MD9LM0xSCVrQiMcNY9hYhwijMiaQ"
VAPID_PRIVATE_KEY="dein-private-key-hier"
VAPID_EMAIL="mailto:stiftung@schloss-wuestenstein.de"

# Claude AI Chatbot
# API-Key von https://console.anthropic.com/
ANTHROPIC_API_KEY="sk-ant-api03-..."

# Brave Search API (für Web-Recherche im Chatbot)
# API-Key von https://brave.com/search/api/ (2000 gratis/Monat)
BRAVE_API_KEY="BSA..."

# Cron-Job Admin Token (für Jahresabschluss-Emails)
# Generiere mit: openssl rand -hex 32
CRON_ADMIN_TOKEN="your-secure-cron-token-here"