# Docker Environment Configuration for Raspberry Pi
#
# IMPORTANT: This is the ONLY credentials file needed for Docker deployment!
# 1. Copy this file to .env.docker: cp .env.docker.example .env.docker
# 2. Set secure passwords (min. 16 characters, mixed case, numbers, special chars)
# 3. Keep .env.docker private (it's in .gitignore)
#
# NOTE: Don't create .env.production or other .env.* files!

# MySQL Configuration
# IMPORTANT: Change these passwords! Never use default values in production!
#
# 💡 Sichere Passwörter im Terminal generieren:
#
# Option 1 (openssl - auf Mac/Linux vorinstalliert):
#   openssl rand -base64 24
#
# Option 2 (nur alphanumerisch, keine Sonderzeichen):
#   openssl rand -hex 20
#
# Option 3 (pwgen - falls installiert: brew install pwgen):
#   pwgen -s 24 1
#
# Tipp: Passwort direkt in Datei schreiben:
#   echo "MYSQL_ROOT_PASSWORD=$(openssl rand -base64 24)" >> .env.docker
#
MYSQL_ROOT_PASSWORD=your_secure_root_password_min_16_chars
MYSQL_DATABASE=nutzerkosten_db
MYSQL_USER=nutzerkosten
MYSQL_PASSWORD=your_secure_password_min_16_chars

# Application Configuration
NODE_ENV=production
HOST=0.0.0.0
PORT=3002

# Database URL
# DO NOT set DATABASE_URL here! It's automatically constructed in docker-compose.yml:
# DATABASE_URL=mysql://${MYSQL_USER}:${MYSQL_PASSWORD}@mysql:3306/${MYSQL_DATABASE}
# This ensures consistency and uses the values from above.

# Magic-Link Authentication
APP_URL=https://www.schloss-wuestenstein.de

# SMTP Settings (für E-Mail-Versand)
SMTP_HOST=your-smtp-host.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
SMTP_FROM=your-email@example.com

# Weather Cache Service (Wetter + Indoor-Temperaturen)
WEATHER_CACHE_URL=http://100.102.83.46:3003/api

# Push Notifications (VAPID Keys)
#
# WICHTIG: Verwende dieselben Keys wie in .env (lokal)!
# Die Keys müssen überall identisch sein, sonst funktionieren
# Push-Subscriptions nicht geräteübergreifend.
#
# Falls noch keine Keys existieren, einmalig generieren:
#   npx web-push generate-vapid-keys
#
# Dann in BEIDE Dateien (.env und .env.docker) eintragen!
#
VAPID_PUBLIC_KEY=BPNNrS_We0xC472MxEYFKhQeaOsvwOkD41X5KPe0sbw43MH2NyXYSYORxJ0MD9LM0xSCVrQiMcNY9hYhwijMiaQ
VAPID_PRIVATE_KEY=your_vapid_private_key_from_env
VAPID_EMAIL=mailto:stiftung@schloss-wuestenstein.de

# Claude AI Chatbot
# API-Key von https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# Brave Search API (für Web-Recherche im Chatbot)
# API-Key von https://brave.com/search/api/ (2000 gratis/Monat)
BRAVE_API_KEY=BSA-your-key-here

# Cron-Job Admin Token (für automatischen Jahresabschluss-Versand)
# Generiere mit: openssl rand -hex 32
# WICHTIG: Muss identisch sein mit dem Token in der Crontab auf dem Pi!
CRON_ADMIN_TOKEN=your-secure-cron-token-here
