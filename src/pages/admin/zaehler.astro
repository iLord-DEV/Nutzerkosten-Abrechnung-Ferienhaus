---
import ProtectedLayout from '../../layouts/ProtectedLayout.astro';
import { requireAdmin } from '../../utils/auth';

// Admin-Berechtigung prüfen
try {
  await requireAdmin(Astro);
} catch (error) {
  return Astro.redirect('/login');
}
---

<ProtectedLayout title="Zählerverwaltung - Admin">
  <div class="section-container">
    <div class="page-header">
      <div class="flex items-center justify-between">
        <div>
          <h1 class="page-title">
            Zählerverwaltung
          </h1>
          <p class="page-description">
            Verwalten Sie die Ölzähler und deren Wechsel
          </p>
        </div>
        <button
          id="neuer-zaehler-btn"
          class="btn btn-primary"
        >
          ➕ Neuer Zähler
        </button>
      </div>
    </div>

    <!-- Zähler-Liste -->
    <div class="card-container">
      <h3 class="text-lg leading-6 font-medium text-base-content mb-2">
        Alle Zähler
      </h3>
      <p class="text-sm text-base-content mb-4" style="opacity: 0.7">
        Das Ausbaudatum wird automatisch auf das Einbaudatum des folgenden Zählers gesetzt.
      </p>
        <div class="overflow-x-auto">
          <table class="table table-zebra">
            <thead>
              <tr>
                <th>Status</th>
                <th>Zähler-ID</th>
                <th>Einbau</th>
                <th>Ausbau</th>
                <th>Letzter Stand</th>
                <th>Tankfüllungen</th>
                <th>Aktionen</th>
              </tr>
            </thead>
            <tbody id="zaehler-tabelle">
              <!-- Wird dynamisch geladen -->
            </tbody>
          </table>
        </div>
    </div>
  </div>

  <!-- Modal für neuen Zähler -->
  <dialog id="zaehler-modal" class="modal">
    <div class="modal-box">
      <h3 class="text-lg font-bold mb-4">
        Neuer Zähler einbauen
      </h3>
      <form id="zaehler-form" class="space-y-4">
        <div class="form-control">
          <label for="einbauDatum" class="label">
            <span class="label-text">Einbaudatum *</span>
          </label>
          <input
            type="date"
            id="einbauDatum"
            name="einbauDatum"
            required
            class="input input-bordered w-full"
          >
        </div>

        <div class="form-control" id="letzter-stand-container" style="display: none;">
          <label for="letzterStandAlterZaehler" class="label">
            <span class="label-text">Ausbaustand des alten Zählers *</span>
          </label>
          <input
            type="number"
            id="letzterStandAlterZaehler"
            name="letzterStandAlterZaehler"
            step="0.1"
            class="input input-bordered w-full"
            placeholder="z.B. 100.0"
          >
          <label class="label">
            <span class="label-text-alt">Der alte Zähler wird automatisch deaktiviert. Das Ausbaudatum wird auf das Einbaudatum des neuen Zählers gesetzt.</span>
          </label>
        </div>

        <div class="form-control">
          <label for="notizen" class="label">
            <span class="label-text">Notizen (optional)</span>
          </label>
          <textarea
            id="notizen"
            name="notizen"
            rows="3"
            class="textarea textarea-bordered w-full"
            placeholder="Grund für den Wechsel, Wartung, etc."
          ></textarea>
        </div>

        <div class="modal-action">
          <button type="button" id="modal-cancel" class="btn btn-ghost">
            Abbrechen
          </button>
          <button type="submit" class="btn btn-primary">
            Zähler einbauen
          </button>
        </div>
      </form>
    </div>
    <form method="dialog" class="modal-backdrop">
      <button>close</button>
    </form>
  </dialog>

  <!-- Modal für Zähler-Details -->
  <div id="zaehler-details-modal" class="hidden">
    <!-- Wird dynamisch gefüllt -->
  </div>
</ProtectedLayout>

<script>
  let zaehler = [];
  let aktiverZaehler = null;

  // Zähler laden
  async function loadZaehler() {
    try {
      const response = await fetch('/api/zaehler');
      if (response.ok) {
        zaehler = await response.json();
        aktiverZaehler = zaehler.find(z => z.istAktiv);
        renderZaehlerTabelle();
      } else {
        console.error('Fehler beim Laden der Zähler');
      }
    } catch (error) {
      console.error('Fehler beim Laden der Zähler:', error);
    }
  }

  // Zähler-Tabelle rendern
  function renderZaehlerTabelle() {
    const tbody = document.getElementById('zaehler-tabelle');
    if (!tbody) return;

    tbody.innerHTML = '';

    zaehler.forEach(zaehler => {
      const row = document.createElement('tr');

      const statusBadge = zaehler.istAktiv
        ? '<span class="badge badge-success">Aktiv</span>'
        : '<span class="badge badge-ghost">Inaktiv</span>';

      const ausbauDatum = zaehler.ausbauDatum
        ? new Date(zaehler.ausbauDatum).toLocaleDateString('de-DE')
        : '-';

      // Letzter Stand - unterschiedlich für aktive/inaktive Zähler
      let letzterStandHtml;
      if (zaehler.istAktiv) {
        // Aktiv: Nur letzter Aufenthalt-Stand
        letzterStandHtml = `${zaehler.letzterAufenthaltStand.toFixed(1)} h`;
      } else {
        // Inaktiv: Beide Werte anzeigen
        letzterStandHtml = `
          <div class="text-sm">
            <div>Letzter Aufenthalt: ${zaehler.letzterAufenthaltStand.toFixed(1)} h</div>
            <div class="text-base-content" style="opacity: 0.7">Ausbau: ${zaehler.letzterStand.toFixed(1)} h</div>
          </div>
        `;
      }

      row.innerHTML = `
        <td>${statusBadge}</td>
        <td>#${zaehler.id}</td>
        <td>${new Date(zaehler.einbauDatum).toLocaleDateString('de-DE')}</td>
        <td>${ausbauDatum}</td>
        <td>${letzterStandHtml}</td>
        <td>${zaehler._count.tankfuellungen}</td>
        <td>
          <button onclick="viewZaehlerDetails(${zaehler.id})" class="btn btn-ghost btn-xs">
            Details
          </button>
          ${!zaehler.istAktiv ? `
            <button onclick="deleteZaehler(${zaehler.id})" class="btn btn-ghost btn-xs text-error">
              Löschen
            </button>
          ` : ''}
        </td>
      `;

      tbody.appendChild(row);
    });
  }

  // Modal öffnen
  function openModal() {
    const modal = document.getElementById('zaehler-modal') as HTMLDialogElement;
    const form = document.getElementById('zaehler-form') as HTMLFormElement;
    const letzterStandContainer = document.getElementById('letzter-stand-container');

    // Formular zurücksetzen
    form.reset();

    // Heute als Standard-Datum setzen
    const heute = new Date().toISOString().split('T')[0];
    (document.getElementById('einbauDatum') as HTMLInputElement).value = heute;

    // Wenn ein aktiver Zähler existiert, Ausbaustand-Feld anzeigen
    const letzterStandInput = document.getElementById('letzterStandAlterZaehler') as HTMLInputElement;
    if (aktiverZaehler) {
      letzterStandContainer.style.display = 'block';
      letzterStandInput.required = true;
      // Letzten Aufenthalt-Stand als Vorschlag eintragen
      if (letzterStandInput && aktiverZaehler.letzterAufenthaltStand) {
        letzterStandInput.value = aktiverZaehler.letzterAufenthaltStand.toFixed(1);
      }
    } else {
      letzterStandContainer.style.display = 'none';
      letzterStandInput.required = false;
    }

    modal.showModal();
  }

  // Modal schließen
  function closeModal() {
    const modal = document.getElementById('zaehler-modal') as HTMLDialogElement;
    modal.close();
  }

  // Neuen Zähler erstellen
  async function createZaehler(formData) {
    try {
      const response = await (window as any).csrfFetch('/api/zaehler', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify(formData),
      });

      if (response.ok) {
        const neuerZaehler = await response.json();
        console.log('Zähler erfolgreich erstellt:', neuerZaehler);
        closeModal();
        loadZaehler(); // Tabelle neu laden
      } else {
        const error = await response.json();
        alert('Fehler beim Erstellen des Zählers: ' + error.error);
      }
    } catch (error) {
      console.error('Fehler beim Erstellen des Zählers:', error);
      alert('Fehler beim Erstellen des Zählers');
    }
  }

  // Zähler-Details anzeigen
  async function viewZaehlerDetails(zaehlerId) {
    try {
      const response = await fetch(`/api/zaehler/${zaehlerId}`);
      if (response.ok) {
        const zaehler = await response.json();
        showZaehlerDetailsModal(zaehler);
      } else {
        alert('Fehler beim Laden der Zähler-Details');
      }
    } catch (error) {
      console.error('Fehler beim Laden der Zähler-Details:', error);
      alert('Fehler beim Laden der Zähler-Details');
    }
  }

  // Zähler-Details Modal anzeigen
  function showZaehlerDetailsModal(zaehler) {
    const modal = document.getElementById('zaehler-details-modal');
    if (!modal) return;

    console.log('📊 Zähler Details:', zaehler);

    // Statistiken berechnen
    const tankfuellungen = zaehler.tankfuellungen || [];
    const aufenthalte = zaehler.aufenthalte || [];

    console.log('⛽ Tankfüllungen:', tankfuellungen.length, tankfuellungen);
    console.log('🏠 Aufenthalte:', aufenthalte.length, aufenthalte);

    const gesamtLiter = tankfuellungen.reduce((sum, tf) => sum + tf.liter, 0);
    const gesamtKosten = tankfuellungen.reduce((sum, tf) => sum + (tf.liter * tf.preisProLiter), 0);

    // Verbrauch berechnen (nur wenn mindestens 2 Tankfüllungen)
    let durchschnittlicherVerbrauch = '-';
    if (tankfuellungen.length >= 2) {
      const ersteTf = tankfuellungen[tankfuellungen.length - 1]; // Älteste (niedrigster Zählerstand)
      const letzteTf = tankfuellungen[0]; // Neueste (höchster Zählerstand)
      const stundenDifferenz = letzteTf.zaehlerstand - ersteTf.zaehlerstand;

      console.log(`🔢 Verbrauchsberechnung:`, {
        ersteTf: ersteTf.zaehlerstand,
        letzteTf: letzteTf.zaehlerstand,
        stundenDifferenz,
        gesamtLiter
      });

      if (stundenDifferenz > 0) {
        durchschnittlicherVerbrauch = `${(gesamtLiter / stundenDifferenz).toFixed(2)} L/h`;
      }
    } else {
      console.log('⚠️ Nicht genug Tankfüllungen für Verbrauchsberechnung:', tankfuellungen.length);
    }

    // Zeitachse erstellen
    const aktivitaeten = [];
    
    // Einbau hinzufügen
    aktivitaeten.push({
      datum: new Date(zaehler.einbauDatum),
      typ: 'einbau',
      text: `Zähler eingebaut`,
      icon: '🔧'
    });

    // Tankfüllungen hinzufügen
    tankfuellungen.forEach(tf => {
      aktivitaeten.push({
        datum: new Date(tf.datum),
        typ: 'tankfuellung',
        text: `${tf.liter}L getankt (€${(tf.liter * tf.preisProLiter).toFixed(2)})`,
        icon: '⛽'
      });
    });

    // Aufenthalte hinzufügen
    aufenthalte.forEach(aufenthalt => {
      aktivitaeten.push({
        datum: new Date(aufenthalt.ankunft),
        typ: 'aufenthalt',
        text: `${aufenthalt.user.name}: ${aufenthalt.zaehlerAbreise - aufenthalt.zaehlerAnkunft}h`,
        icon: '🏠'
      });
    });

    // Ausbau hinzufügen (falls vorhanden)
    if (zaehler.ausbauDatum) {
      aktivitaeten.push({
        datum: new Date(zaehler.ausbauDatum),
        typ: 'ausbau',
        text: `Zähler ausgebaut (Ausbaustand: ${zaehler.letzterStand.toFixed(1)}h, Letzter Aufenthalt: ${zaehler.letzterAufenthaltStand.toFixed(1)}h)`,
        icon: '📦'
      });
    }

    // Nach Datum sortieren
    aktivitaeten.sort((a, b) => a.datum - b.datum);

    // Modal-Inhalt erstellen
    modal.innerHTML = `
      <dialog id="zaehler-details-dialog" class="modal modal-open">
        <div class="modal-box w-11/12 max-w-4xl">
          <div class="flex justify-between items-center mb-4">
            <h3 class="text-xl font-bold">
              Zähler #${zaehler.id} - Details
            </h3>
            <button onclick="closeZaehlerDetailsModal()" class="btn btn-sm btn-circle btn-ghost">✕</button>
          </div>

          <!-- Grunddaten -->
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6">
            <div class="bg-base-200 p-4 rounded-lg">
              <h4 class="font-medium text-base-content mb-3">Grunddaten</h4>
              <div class="space-y-2 text-sm">
                <div class="flex justify-between">
                  <span class="opacity-70">Status:</span>
                  <span class="badge ${zaehler.istAktiv ? 'badge-success' : 'badge-ghost'}">
                    ${zaehler.istAktiv ? 'Aktiv' : 'Inaktiv'}
                  </span>
                </div>
                <div class="flex justify-between">
                  <span class="opacity-70">Einbaudatum:</span>
                  <span class="font-medium">${new Date(zaehler.einbauDatum).toLocaleDateString('de-DE')}</span>
                </div>
                <div class="flex justify-between">
                  <span class="opacity-70">Ausbaudatum:</span>
                  <span class="font-medium">${zaehler.ausbauDatum ? new Date(zaehler.ausbauDatum).toLocaleDateString('de-DE') : 'Noch aktiv'}</span>
                </div>
                <div class="flex justify-between">
                  <span class="opacity-70">Letzter Aufenthalt-Stand:</span>
                  <span class="font-medium">${zaehler.letzterAufenthaltStand.toFixed(1)} h</span>
                </div>
                ${!zaehler.istAktiv ? `
                  <div class="flex justify-between">
                    <span class="opacity-70">Ausbaustand:</span>
                    <span class="font-medium">${zaehler.letzterStand.toFixed(1)} h</span>
                  </div>
                ` : ''}
                ${zaehler.notizen ? `
                  <div class="mt-3 pt-3 border-t border-base-300">
                    <span class="opacity-70">Notizen:</span>
                    <p class="text-sm mt-1">${zaehler.notizen}</p>
                  </div>
                ` : ''}
              </div>
            </div>

            <div class="bg-base-200 p-4 rounded-lg">
              <h4 class="font-medium text-base-content mb-3">Statistiken</h4>
              <div class="space-y-2 text-sm">
                <div class="flex justify-between">
                  <span class="opacity-70">Tankfüllungen:</span>
                  <span class="font-medium">${tankfuellungen.length}</span>
                </div>
                <div class="flex justify-between">
                  <span class="opacity-70">Aufenthalte:</span>
                  <span class="font-medium">${aufenthalte.length}</span>
                </div>
                <div class="flex justify-between">
                  <span class="opacity-70">Gesamt Liter:</span>
                  <span class="font-medium">${gesamtLiter.toFixed(1)} L</span>
                </div>
                <div class="flex justify-between">
                  <span class="opacity-70">Gesamt Kosten:</span>
                  <span class="font-medium">€${gesamtKosten.toFixed(2)}</span>
                </div>
                <div class="flex justify-between">
                  <span class="opacity-70">Ø Verbrauch:</span>
                  <span class="font-medium">${durchschnittlicherVerbrauch}</span>
                </div>
              </div>
            </div>
          </div>

          <!-- Zeitachse -->
          <div class="bg-base-200 p-4 rounded-lg">
            <h4 class="font-medium text-base-content mb-3">Zeitachse</h4>
            <div class="space-y-3">
              ${aktivitaeten.map(aktivitaet => `
                <div class="flex items-center space-x-3">
                  <span class="text-lg">${aktivitaet.icon}</span>
                  <div class="flex-1">
                    <div class="text-sm font-medium">
                      ${aktivitaet.text}
                    </div>
                    <div class="text-xs opacity-70">
                      ${aktivitaet.datum.toLocaleDateString('de-DE')} ${aktivitaet.datum.toLocaleTimeString('de-DE', {hour: '2-digit', minute: '2-digit'})}
                    </div>
                  </div>
                </div>
              `).join('')}
            </div>
          </div>

          <div class="modal-action">
            <button onclick="closeZaehlerDetailsModal()" class="btn btn-ghost">
              Schließen
            </button>
          </div>
        </div>
        <form method="dialog" class="modal-backdrop">
          <button onclick="closeZaehlerDetailsModal()">close</button>
        </form>
      </dialog>
    `;
  }

  // Zähler-Details Modal schließen
  function closeZaehlerDetailsModal() {
    const modal = document.getElementById('zaehler-details-modal');
    if (modal) {
      modal.innerHTML = '';
    }
  }

  // Zähler löschen
  async function deleteZaehler(zaehlerId) {
    if (!confirm('Sind Sie sicher, dass Sie diesen Zähler löschen möchten?')) {
      return;
    }

    try {
      const response = await (window as any).csrfFetch(`/api/zaehler/${zaehlerId}`, {
        method: 'DELETE',
      });

      if (response.ok) {
        console.log('Zähler erfolgreich gelöscht');
        loadZaehler(); // Tabelle neu laden
      } else {
        const error = await response.json();
        alert('Fehler beim Löschen des Zählers: ' + error.error);
      }
    } catch (error) {
      console.error('Fehler beim Löschen des Zählers:', error);
      alert('Fehler beim Löschen des Zählers');
    }
  }

  // Event-Listener
  document.addEventListener('DOMContentLoaded', () => {
    loadZaehler();

    // Modal-Buttons
    document.getElementById('neuer-zaehler-btn')?.addEventListener('click', openModal);
    document.getElementById('modal-cancel')?.addEventListener('click', closeModal);

    // Form-Submit
    document.getElementById('zaehler-form')?.addEventListener('submit', (e) => {
      e.preventDefault();

      const formData = new FormData(e.target as HTMLFormElement);
      const data = Object.fromEntries(formData.entries());

      // Wenn ein aktiver Zähler existiert, wird er automatisch ersetzt
      data.ersetzeAktiven = aktiverZaehler ? true : false;

      createZaehler(data);
    });
  });

  // Globale Funktionen für onclick-Handler
  window.viewZaehlerDetails = viewZaehlerDetails;
  window.deleteZaehler = deleteZaehler;
  window.closeZaehlerDetailsModal = closeZaehlerDetailsModal;
</script>
