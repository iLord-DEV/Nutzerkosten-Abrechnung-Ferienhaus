---
import ProtectedLayout from '../layouts/ProtectedLayout.astro';
import { getUser } from '../utils/auth';

// Authentifizierung prüfen
const user = await getUser(Astro);
if (!user) {
  return Astro.redirect('/login');
}
---

<ProtectedLayout title="Dashboard - Nutzerkosten">
  <div class="px-4 sm:px-6 lg:px-8">
    <div class="mb-8">
      <div class="flex items-center justify-between">
        <div>
          <h1 class="text-3xl font-bold">
            Mein Dashboard
          </h1>
          <p class="mt-2 text-base-content/70">
            Willkommen, <span id="user-name">Benutzer</span>! Hier findest du deine persönlichen Aufenthalte und Kosten.
          </p>
        </div>

      </div>
    </div>

    <!-- Pending Termine Alert -->
    <div id="pending-termine-alert" class="hidden mb-6">
      <div class="alert bg-warning/10 border-l-4 border-warning shadow">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6 text-warning shrink-0">
          <path stroke-linecap="round" stroke-linejoin="round" d="M6.75 3v2.25M17.25 3v2.25M3 18.75V7.5a2.25 2.25 0 012.25-2.25h13.5A2.25 2.25 0 0121 7.5v11.25m-18 0A2.25 2.25 0 005.25 21h13.5A2.25 2.25 0 0021 18.75m-18 0v-7.5A2.25 2.25 0 015.25 9h13.5A2.25 2.25 0 0121 11.25v7.5" />
        </svg>
        <div class="flex-1">
          <h3 class="font-bold">Termine warten auf deine Abstimmung</h3>
          <p id="pending-termine-text" class="text-sm opacity-80"></p>
        </div>
        <a href="/terminplanung" class="btn btn-warning btn-sm">Jetzt abstimmen</a>
      </div>
    </div>

    <!-- Temperatur-Anzeige -->
    <div class="card bg-base-100 shadow-xl mb-8">
      <div class="card-body">
        <h2 class="card-title">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6.75 7.5l3 2.25-3 2.25m4.5 0h3m-9 8.25h13.5A2.25 2.25 0 0021 18V6a2.25 2.25 0 00-2.25-2.25H5.25A2.25 2.25 0 003 6v12a2.25 2.25 0 002.25 2.25z" />
          </svg>
          Aktuelle Temperaturen
        </h2>
        <div class="stats stats-horizontal shadow mt-4">
          <div class="stat">
            <div class="stat-figure">
              <i class="wi wi-thermometer text-3xl" id="temp-bad-icon"></i>
            </div>
            <div class="stat-title">Bad</div>
            <div class="stat-value" id="temp-bad">--°C</div>
            <div class="stat-desc" id="temp-bad-update">Lädt...</div>
          </div>

          <div class="stat">
            <div class="stat-figure">
              <i class="wi wi-thermometer text-3xl" id="temp-wohnzimmer-icon"></i>
            </div>
            <div class="stat-title">Wohnzimmer</div>
            <div class="stat-value" id="temp-wohnzimmer">--°C</div>
            <div class="stat-desc" id="temp-wohnzimmer-update">Lädt...</div>
          </div>
        </div>

        <!-- Wetter & Außentemperatur (ausklappbar) -->
        <div class="collapse collapse-arrow bg-base-200 mt-6">
          <input type="checkbox" />
          <div class="collapse-title text-lg font-semibold flex items-center justify-between">
            <div class="flex items-center gap-2">
              <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M2.25 15a4.5 4.5 0 004.5 4.5H18a3.75 3.75 0 001.332-7.257 3 3 0 00-3.758-3.848 5.25 5.25 0 00-10.233 2.33A4.502 4.502 0 002.25 15z" />
              </svg>
              Wetter & Außentemperatur
            </div>
            <span class="text-2xl font-bold text-primary" id="weather-temp-preview">--°C</span>
          </div>
          <div class="collapse-content">
        <!-- Wetterwarnungen (falls vorhanden) -->
        <div id="weather-alerts" class="hidden mb-4">
          <div class="alert bg-base-100 border-l-4 border-secondary shadow">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" class="stroke-secondary shrink-0 w-6 h-6"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" /></svg>
            <div id="weather-alerts-content"></div>
          </div>
        </div>

        <!-- Aktuelles Wetter -->
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
          <!-- Hauptwetter -->
          <div class="flex items-center gap-6">
            <div class="flex-shrink-0">
              <i id="weather-icon" class="wi wi-na text-6xl text-primary"></i>
            </div>
            <div>
              <div class="text-4xl font-bold" id="weather-temp">--°C</div>
              <div class="text-lg opacity-70" id="weather-description">Lädt...</div>
              <div class="text-sm opacity-70" id="weather-feels-like">Gefühlt: --°C</div>
            </div>
          </div>

          <!-- Wetter-Details -->
          <div class="grid grid-cols-2 gap-4">
            <div>
              <div class="text-sm opacity-70">Wind</div>
              <div class="font-semibold" id="weather-wind">-- km/h</div>
            </div>
            <div>
              <div class="text-sm opacity-70">Luftfeuchtigkeit</div>
              <div class="font-semibold" id="weather-humidity">--%</div>
            </div>
            <div>
              <div class="text-sm opacity-70">UV-Index</div>
              <div class="font-semibold" id="weather-uv">--</div>
            </div>
            <div>
              <div class="text-sm opacity-70">Sichtweite</div>
              <div class="font-semibold" id="weather-visibility">-- km</div>
            </div>
          </div>
        </div>

        <!-- Vorhersage nächste Stunden -->
        <div class="mt-6">
          <h3 class="text-lg font-semibold mb-3">Nächste Stunden</h3>
          <div class="grid grid-cols-4 md:grid-cols-6 lg:grid-cols-8 gap-3" id="weather-hourly">
            <!-- Wird dynamisch geladen -->
          </div>
        </div>

        <!-- Vorhersage nächste Tage -->
        <div class="mt-6">
          <h3 class="text-lg font-semibold mb-3">7-Tage Vorhersage</h3>
          <div class="grid grid-cols-1 gap-2" id="weather-daily">
            <!-- Wird dynamisch geladen -->
          </div>
        </div>

            <div class="text-xs opacity-70 mt-4" id="weather-update">
              Lädt...
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Persönliche Übersichtskarten -->
    <div class="stats stats-vertical lg:stats-horizontal shadow w-full mb-8">
      <div class="stat">
        <div class="stat-title">Meine Aufenthalte</div>
        <div class="stat-value text-primary" id="my-aufenthalte">-</div>
      </div>

      <div class="stat">
        <div class="stat-title">Aufenthalte insgesamt</div>
        <div class="stat-value text-primary" id="total-aufenthalte">-</div>
      </div>

      <div class="stat">
        <div class="stat-title">Letzter Zählerstand</div>
        <div class="stat-value text-primary" id="last-zaehlerstand">-</div>
      </div>

      <div class="stat">
        <div class="stat-title flex items-center gap-2">
          Meine Kosten <span id="kosten-jahr">2024</span>
        </div>
        <div class="stat-value text-primary" id="my-kosten">-</div>
        <div class="stat-desc">Laufendes Jahr</div>
      </div>
    </div>

    <!-- Schnellaktionen für Benutzer -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 mb-8">
      <!-- Meine Aktionen -->
      <div class="card bg-base-100 shadow-xl">
        <div class="card-body">
          <h2 class="card-title">Meine Aktionen</h2>
          <div class="space-y-2">
            <a href="/aufenthalte/neu" class="btn btn-primary w-full">
              Neuen Aufenthalt erfassen
            </a>
            <a href="/statistiken" class="btn btn-ghost w-full">
              Meine Statistiken anzeigen
            </a>
            <a href="/statistiken?gesamt=true" class="btn btn-ghost w-full">
              Gesamtübersicht aller Nutzer
            </a>
            <a href="/aufenthalte" class="btn btn-ghost w-full">
              Alle meine Aufenthalte
            </a>
            <a href="/profil/passwort-aendern" class="btn btn-ghost w-full">
              Passwort ändern
            </a>
          </div>
        </div>
      </div>

      <!-- Letzte Aktivitäten -->
      <div class="card bg-base-100 shadow-xl">
        <div class="card-body">
          <h2 class="card-title">Meine letzten Aktivitäten</h2>
          <div class="flow-root">
            <ul role="list" class="-mb-8" id="my-aktivitaeten-liste">
              <!-- Wird dynamisch geladen -->
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
</ProtectedLayout>

<script>
  // Benutzer-Dashboard Funktionalität
  let currentUser: any = null;

  // Benutzer-Informationen laden
  async function loadUserInfo() {
    try {
      const response = await fetch('/api/auth/me');
      if (response.ok) {
        currentUser = await response.json();
        const userNameEl = document.getElementById('user-name');
        if (userNameEl) {
          userNameEl.textContent = currentUser.name;
        }
        
        // Dashboard-Daten laden
        loadDashboardData();
        loadMyAktivitaeten();
      } else {
        // Nicht eingeloggt, zurück zum Login
        window.location.href = '/login';
      }
    } catch (error) {
      console.error('Fehler beim Laden der Benutzer-Informationen:', error);
      window.location.href = '/login';
    }
  }

  // Dashboard-Daten laden
  async function loadDashboardData() {
    try {
      const currentYear = new Date().getFullYear();
      const kostenJahrEl = document.getElementById('kosten-jahr');
      if (kostenJahrEl) kostenJahrEl.textContent = currentYear.toString();

      // Persönliche Statistiken laden
      const statsResponse = await fetch(`/api/statistiken?jahr=${currentYear}`);
      if (statsResponse.ok) {
        const stats = await statsResponse.json();

        const myAufenthalteEl = document.getElementById('my-aufenthalte');
        const myTankfuellungenEl = document.getElementById('my-tankfuellungen');
        const myKostenEl = document.getElementById('my-kosten');

        if (myAufenthalteEl) myAufenthalteEl.textContent = stats.anzahlAufenthalte || '0';
        if (myTankfuellungenEl) myTankfuellungenEl.textContent = '0'; // Normale Benutzer haben keine Tankfüllungen
        if (myKostenEl) myKostenEl.textContent = `€ ${(stats.gesamtKosten || 0).toFixed(2)}`;
      }


      // Gesamtanzahl Aufenthalte laden und letzten Zählerstand ermitteln
      const aufenthalteResponse = await fetch(`/api/aufenthalte?jahr=${currentYear}`);
      if (aufenthalteResponse.ok) {
        const aufenthalte = await aufenthalteResponse.json();
        const totalAufenthalteEl = document.getElementById('total-aufenthalte');
        if (totalAufenthalteEl) totalAufenthalteEl.textContent = aufenthalte.length.toString();

        // Letzten Zählerstand aus dem neuesten Aufenthalt holen
        if (aufenthalte.length > 0) {
          // Aufenthalte nach Abreisedatum sortieren (neueste zuerst)
          const sortedAufenthalte = aufenthalte.sort((a: any, b: any) =>
            new Date(b.abreise).getTime() - new Date(a.abreise).getTime()
          );
          const neuesterAufenthalt = sortedAufenthalte[0];
          const lastZaehlerstandEl = document.getElementById('last-zaehlerstand');
          if (lastZaehlerstandEl && neuesterAufenthalt.zaehlerAbreise) {
            lastZaehlerstandEl.textContent = `${neuesterAufenthalt.zaehlerAbreise} h`;
          }
        }
      }
    } catch (error) {
      console.error('Fehler beim Laden der Dashboard-Daten:', error);
    }
  }

  // Meine Aktivitäten laden
  async function loadMyAktivitaeten() {
    try {
      const aufenthalteResponse = await fetch('/api/aufenthalte');

      if (aufenthalteResponse.ok) {
        const aufenthalte = await aufenthalteResponse.json();
        const myAufenthalte = aufenthalte.filter((a: any) => a.userId === currentUser.id);

        // Normale Benutzer haben keine Tankfüllungen
        const myTankfuellungen: any[] = [];

        renderAktivitaeten(myAufenthalte, myTankfuellungen);
      }
    } catch (error) {
      console.error('Fehler beim Laden der Aktivitäten:', error);
    }
  }

  // Aktivitäten rendern
  function renderAktivitaeten(aufenthalte: any[], tankfuellungen: any[]) {
    const ul = document.getElementById('my-aktivitaeten-liste');
    if (!ul) return;

    ul.innerHTML = '';

    const alleAktivitaeten = [
      ...aufenthalte.map((a: any) => ({
        type: 'aufenthalt',
        datum: new Date(a.ankunft),
        dateRange: `${new Date(a.ankunft).toLocaleDateString('de-DE')} - ${new Date(a.abreise).toLocaleDateString('de-DE')}`,
        text: `Aufenthalt`
      })),
      ...tankfuellungen.map((t: any) => ({
        type: 'tankfuellung',
        datum: new Date(t.datum),
        dateRange: new Date(t.datum).toLocaleDateString('de-DE'),
        text: `Tankfüllung: ${t.liter}L`
      }))
    ];

    // Nach Datum sortieren (neueste zuerst)
    alleAktivitaeten.sort((a: any, b: any) => b.datum.getTime() - a.datum.getTime());

    // Nur die letzten 5 anzeigen
    alleAktivitaeten.slice(0, 5).forEach((aktivitaet: any, index: number) => {
      const li = document.createElement('li');
      li.className = 'relative pb-8';

      const badgeColor = 'badge-primary';
      const isLast = index === Math.min(alleAktivitaeten.length, 5) - 1;

      li.innerHTML = `
        <div class="relative flex space-x-3">
          <div>
            <span class="badge ${badgeColor} badge-lg"></span>
          </div>
          <div class="min-w-0 flex-1 flex justify-between space-x-4">
            <div>
              <p class="text-sm font-medium">${aktivitaet.text}</p>
            </div>
            <div class="text-right text-sm whitespace-nowrap opacity-70">
              <time>${aktivitaet.dateRange}</time>
            </div>
          </div>
        </div>
        ${!isLast ? '<div class="absolute left-2 top-8 -ml-px h-full w-0.5 bg-base-300" aria-hidden="true"></div>' : ''}
      `;

      ul.appendChild(li);
    });

    if (alleAktivitaeten.length === 0) {
      ul.innerHTML = `
        <li class="text-center opacity-70 py-4">
          Noch keine Aktivitäten
        </li>
      `;
    }
  }



  // Temperaturen laden
  async function loadTemperaturen() {
    try {
      const response = await fetch('/api/temperaturen');
      if (response.ok) {
        const data = await response.json();

        const tempBadEl = document.getElementById('temp-bad');
        const tempWohnzimmerEl = document.getElementById('temp-wohnzimmer');
        const tempBadUpdateEl = document.getElementById('temp-bad-update');
        const tempWohnzimmerUpdateEl = document.getElementById('temp-wohnzimmer-update');

        if (tempBadEl) {
          tempBadEl.textContent = data.bad !== null ? `${data.bad.toFixed(1)}°C` : 'N/A';
          if (data.bad !== null) {
            const color = getTempColor(data.bad);
            tempBadEl.style.color = color;
            const badIcon = document.getElementById('temp-bad-icon');
            if (badIcon) badIcon.style.color = color;
          }
        }
        if (tempWohnzimmerEl) {
          tempWohnzimmerEl.textContent = data.wohnzimmer !== null ? `${data.wohnzimmer.toFixed(1)}°C` : 'N/A';
          if (data.wohnzimmer !== null) {
            const color = getTempColor(data.wohnzimmer);
            tempWohnzimmerEl.style.color = color;
            const wohnzimmerIcon = document.getElementById('temp-wohnzimmer-icon');
            if (wohnzimmerIcon) wohnzimmerIcon.style.color = color;
          }
        }

        // Update-Zeitpunkt anzeigen
        if (tempBadUpdateEl && data.lastUpdate?.bad) {
          const updateTime = new Date(data.lastUpdate.bad);
          tempBadUpdateEl.textContent = `Aktualisiert: ${updateTime.toLocaleTimeString('de-DE', { hour: '2-digit', minute: '2-digit' })}`;
        }
        if (tempWohnzimmerUpdateEl && data.lastUpdate?.wohnzimmer) {
          const updateTime = new Date(data.lastUpdate.wohnzimmer);
          tempWohnzimmerUpdateEl.textContent = `Aktualisiert: ${updateTime.toLocaleTimeString('de-DE', { hour: '2-digit', minute: '2-digit' })}`;
        }
      }
    } catch (error) {
      console.error('Fehler beim Laden der Temperaturen:', error);
      const tempBadEl = document.getElementById('temp-bad');
      const tempWohnzimmerEl = document.getElementById('temp-wohnzimmer');
      if (tempBadEl) tempBadEl.textContent = 'Fehler';
      if (tempWohnzimmerEl) tempWohnzimmerEl.textContent = 'Fehler';
    }
  }

  // OpenWeatherMap Icon zu Weather Icons Mapping
  function getWeatherIconClass(owmIcon: string): string {
    const iconMap: Record<string, string> = {
      '01d': 'wi-day-sunny',
      '01n': 'wi-night-clear',
      '02d': 'wi-day-cloudy',
      '02n': 'wi-night-alt-cloudy',
      '03d': 'wi-cloud',
      '03n': 'wi-cloud',
      '04d': 'wi-cloudy',
      '04n': 'wi-cloudy',
      '09d': 'wi-showers',
      '09n': 'wi-showers',
      '10d': 'wi-day-rain',
      '10n': 'wi-night-alt-rain',
      '11d': 'wi-thunderstorm',
      '11n': 'wi-thunderstorm',
      '13d': 'wi-snow',
      '13n': 'wi-snow',
      '50d': 'wi-fog',
      '50n': 'wi-fog',
    };
    return iconMap[owmIcon] || 'wi-na';
  }

  // Temperatur-Farbcodierung (HSL) - WCAG-konform für Light & Dark Mode
  function getTempColor(temp: number): string {
    // Clamp zwischen -10 und +30
    const t = Math.max(-10, Math.min(30, temp));
    let hue;
    if (t <= 0) {
      // -10°C bis 0°C: Blau bleibt blau (220-200)
      hue = 220 - ((t + 10) / 10) * 20;
    } else if (t <= 10) {
      // 0°C bis +10°C: Blau (200) bis Grün (120)
      hue = 200 - (t / 10) * 80;
    } else {
      // +10°C bis +30°C: Grün (120) bis Rot (0)
      hue = 120 - ((t - 10) / 20) * 120;
    }
    // Lightness angepasst für besseren Kontrast in beiden Themes
    const isDark = document.documentElement.getAttribute('data-theme') === 'dark';
    const lightness = isDark ? 65 : 40;
    return `hsl(${Math.round(hue)}, 75%, ${lightness}%)`;
  }

  // Farbe basierend auf Wetter-Icon - WCAG-konform
  function getWeatherIconColor(owmIcon: string): string {
    const colorMap: Record<string, string> = {
      '01d': 'bg-gradient-to-br from-yellow-400 to-orange-500 bg-clip-text text-transparent', // Sonne
      '01n': 'text-indigo-400', // Nacht klar (dunkler für Kontrast)
      '02d': 'bg-gradient-to-br from-yellow-400 to-slate-400 bg-clip-text text-transparent', // Sonne + Wolke
      '02n': 'text-slate-500', // Nacht bewölkt
      '03d': 'text-slate-500', // Wolken
      '03n': 'text-slate-400',
      '04d': 'text-slate-600', // Bedeckt
      '04n': 'text-slate-500',
      '09d': 'text-blue-500', // Schauer
      '09n': 'text-blue-400',
      '10d': 'bg-gradient-to-b from-slate-400 to-blue-500 bg-clip-text text-transparent', // Regen
      '10n': 'text-blue-500',
      '11d': 'bg-gradient-to-b from-slate-500 to-yellow-400 bg-clip-text text-transparent', // Gewitter
      '11n': 'text-purple-400',
      '13d': 'text-sky-500', // Schnee (dunkler für Kontrast)
      '13n': 'text-sky-400',
      '50d': 'text-slate-500', // Nebel
      '50n': 'text-slate-400',
    };
    return colorMap[owmIcon] || 'text-primary';
  }

  // Wetter laden
  async function loadWetter() {
    try {
      const response = await fetch('/api/wetter');
      if (response.ok) {
        const data = await response.json();

        // Alerts anzeigen (falls vorhanden)
        const alertsDiv = document.getElementById('weather-alerts');
        const alertsContent = document.getElementById('weather-alerts-content');
        if (data.alerts && data.alerts.length > 0) {
          if (alertsContent) {
            alertsContent.innerHTML = data.alerts.map((alert: any, index: number) => {
              const startDate = new Date(alert.start);
              const endDate = new Date(alert.end);
              const formatDate = (date: Date) => date.toLocaleString('de-DE', {
                day: '2-digit',
                month: '2-digit',
                hour: '2-digit',
                minute: '2-digit'
              });

              // Ersten Buchstaben des Events großschreiben
              const eventName = alert.event.charAt(0).toUpperCase() + alert.event.slice(1);

              return `
                <div>
                  <div class="font-semibold">${eventName} (Warnung ${index + 1}/${data.alerts.length})</div>
                  <div class="text-xs opacity-70 mb-1">${formatDate(startDate)} - ${formatDate(endDate)}</div>
                  <div class="text-sm">${alert.description}</div>
                </div>
              `;
            }).join('<div class="divider my-2"></div>');
          }
          if (alertsDiv) alertsDiv.classList.remove('hidden');
        }

        // Aktuelles Wetter
        const current = data.current;
        const tempEl = document.getElementById('weather-temp');
        const tempPreviewEl = document.getElementById('weather-temp-preview');
        const descEl = document.getElementById('weather-description');
        const feelsLikeEl = document.getElementById('weather-feels-like');
        const iconEl = document.getElementById('weather-icon');
        const windEl = document.getElementById('weather-wind');
        const humidityEl = document.getElementById('weather-humidity');
        const uvEl = document.getElementById('weather-uv');
        const visibilityEl = document.getElementById('weather-visibility');
        const updateEl = document.getElementById('weather-update');

        if (tempEl) {
          tempEl.textContent = `${current.temp}°C`;
          tempEl.style.color = getTempColor(current.temp);
        }
        if (tempPreviewEl) {
          tempPreviewEl.textContent = `${current.temp}°C`;
          tempPreviewEl.style.color = getTempColor(current.temp);
        }
        if (descEl) descEl.textContent = current.description.charAt(0).toUpperCase() + current.description.slice(1);
        if (feelsLikeEl) feelsLikeEl.textContent = `Gefühlt: ${current.feelsLike}°C`;
        if (iconEl) {
          iconEl.className = `wi ${getWeatherIconClass(current.icon)} text-6xl`;
          iconEl.style.color = getTempColor(current.temp);
        }
        if (windEl) windEl.textContent = `${current.windSpeed} km/h`;
        if (humidityEl) humidityEl.textContent = `${current.humidity}%`;
        if (uvEl) {
          uvEl.textContent = current.uvIndex.toString();
          // UV-Index Farbcodierung
          const uvValue = current.uvIndex;
          if (uvValue <= 2) uvEl.className = 'font-semibold text-success';
          else if (uvValue <= 5) uvEl.className = 'font-semibold text-warning';
          else if (uvValue <= 7) uvEl.className = 'font-semibold text-orange-500';
          else uvEl.className = 'font-semibold text-error';
        }
        if (visibilityEl) visibilityEl.textContent = `${current.visibility} km`;
        if (updateEl) {
          const time = new Date(current.timestamp);
          updateEl.textContent = `Aktualisiert: ${time.toLocaleString('de-DE')}`;
        }

        // Stündliche Vorhersage (nächste 8 Stunden)
        const hourlyDiv = document.getElementById('weather-hourly');
        if (hourlyDiv && data.hourly) {
          hourlyDiv.innerHTML = data.hourly.slice(0, 8).map((hour: any) => {
            const time = new Date(hour.time);
            return `
              <div class="text-center p-2 bg-base-100 border border-base-300 rounded-lg">
                <div class="text-xs opacity-70">${time.getHours()}:00</div>
                <i class="wi ${getWeatherIconClass(hour.icon)} text-3xl" style="color: ${getTempColor(hour.temp)}"></i>
                <div class="font-semibold" style="color: ${getTempColor(hour.temp)}">${hour.temp}°C</div>
                ${hour.pop > 0 ? `<div class="text-xs text-info">${hour.pop}%</div>` : ''}
              </div>
            `;
          }).join('');
        }

        // Tägliche Vorhersage
        const dailyDiv = document.getElementById('weather-daily');
        if (dailyDiv && data.daily) {
          const weekdays = ['So', 'Mo', 'Di', 'Mi', 'Do', 'Fr', 'Sa'];
          dailyDiv.innerHTML = data.daily.map((day: any) => {
            const date = new Date(day.date);
            const weekday = weekdays[date.getDay()];
            const avgTemp = (day.tempMin + day.tempMax) / 2;
            return `
              <div class="flex items-center justify-between p-3 bg-base-100 border border-base-300 rounded-lg">
                <div class="flex items-center gap-3 flex-1">
                  <div class="w-16 text-sm font-semibold">${weekday}, ${date.getDate()}.${date.getMonth() + 1}.</div>
                  <i class="wi ${getWeatherIconClass(day.icon)} text-2xl w-8" style="color: ${getTempColor(avgTemp)}"></i>
                  <div class="flex-1 text-sm opacity-70">${day.description.charAt(0).toUpperCase() + day.description.slice(1)}</div>
                </div>
                <div class="flex items-center gap-4">
                  ${day.pop > 0 ? `<div class="text-sm text-info">${day.pop}%</div>` : ''}
                  <div class="text-sm" style="color: ${getTempColor(day.tempMin)}">${day.tempMin}°</div>
                  <div class="font-semibold" style="color: ${getTempColor(day.tempMax)}">${day.tempMax}°C</div>
                </div>
              </div>
            `;
          }).join('');
        }
      }
    } catch (error) {
      console.error('Fehler beim Laden der Wetterdaten:', error);
    }
  }

  // Pending Termine Alert laden
  async function loadPendingTermineAlert() {
    try {
      const response = await fetch('/api/terminplanung/pending');
      if (response.ok) {
        const data = await response.json();
        const alertEl = document.getElementById('pending-termine-alert');
        const textEl = document.getElementById('pending-termine-text');
        if (alertEl && textEl) {
          if (data.count > 0) {
            const terminTitel = data.termine.map((t: any) => t.titel).join(', ');
            textEl.textContent = `${data.count} Termin${data.count > 1 ? 'e' : ''}: ${terminTitel}`;
            alertEl.classList.remove('hidden');
          } else {
            alertEl.classList.add('hidden');
          }
        }
      }
    } catch (error) {
      // Alert-Fehler ist nicht kritisch
    }
  }

  // Event-Listener
  document.addEventListener('DOMContentLoaded', () => {
    loadUserInfo();
    loadTemperaturen();
    loadWetter();
    loadPendingTermineAlert();

    // Temperaturen und Wetter alle 10 Minuten aktualisieren
    setInterval(() => {
      loadTemperaturen();
      loadWetter();
    }, 10 * 60 * 1000);

    // Listen for pending termine changes (from Terminplanung page)
    window.addEventListener('pending-termine-changed', () => {
      loadPendingTermineAlert();
    });
  });
</script>
</ProtectedLayout>
